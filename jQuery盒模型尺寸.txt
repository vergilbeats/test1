width()
取width

innerWidth() 方法返回第一个匹配元素的内部宽度。
该方法包含 padding，但不包含 border 和 margin

outerWidth()函数用于获取当前匹配元素的外宽度。外宽度默认包括元素的内边距(padding)、边框(border)，但不包括外边距(margin)部分的宽度。你也可以指定参数为true，以包括外边距(margin)部分的宽度

height()系列同上